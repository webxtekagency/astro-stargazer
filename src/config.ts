/**
 * Stargazer package — central config
 * Change these values here and they propagate everywhere in the package.
 */

/** Public website URL — used for the docs iframe and external links. */
export const SITE_URL = 'https://astro-stargazer.pages.dev';

/** Full URL to the documentation page. */
export const DOCS_URL = `${SITE_URL}/documentation`;

/** Webxtek agency URL — used in footer credits. */
export const WEBXTEK_URL = 'https://webxtek.com';
