---
export const prerender = false;

import { DOCS_URL } from '../config';

// Redirect immediately to the official docs website.
// An iframe does not work cross-origin (X-Frame-Options: SAMEORIGIN),
// so this is the correct, universally compatible approach.
return Astro.redirect(DOCS_URL, 302);
---
